import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const quizMarkSlice = createSlice({
  name: "quizMarkSlice",
  initialState,
  reducers: {},
});

export const {} = quizMarkSlice.actions;
export default quizMarkSlice.reducer;
